{
  "name": "travel-destinations",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "biome format . --write",
    "start:api": "NODE_ENV=production pnpm --filter=api start",
    "start:web": "pnpm --filter=web start",
    "start": "cross-env NODE_ENV=production pnpm --stream -r run start",
    "dev": "NODE_ENV=development pnpm --stream -r run dev",
    "prepare": "husky && husky install",
    "build": "pnpm --filter=web build && pnpm --filter=api build"
  },
  "workspaces": ["apps/*"],
  "author": "",
  "engines": {
    "node": ">=18.16.1"
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "^1.9.1",
    "@eslint/js": "^8.56.0",
    "@html-eslint/eslint-plugin": "^0.24.0",
    "@html-eslint/parser": "^0.24.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.56.0",
    "eslint-config-extreme": "^1.0.22",
    "husky": "^8.0.0",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "*.js": ["biome format --write", "biome lint --write", "eslint --fix"],
    "*.{json,md,yml}": ["biome format --write"]
  }
}
