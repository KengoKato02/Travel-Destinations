{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "start": "cross-env NODE_ENV=production http-server . -p 8080 -c-1",
    "dev": "cross-env NODE_ENV=development http-server . -p 8080 -c-1",
    "build": "mkdir -p dist && find . -maxdepth 1 ! -name 'dist' -exec cp -r {} dist/ \\;"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
